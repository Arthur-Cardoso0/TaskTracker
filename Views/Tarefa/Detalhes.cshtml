@model TaskTracker.Models.Tarefa
@{
    ViewData["Title"] = "Detalhes da Tarefa";
}

@functions 
{
    public string FormatarStatus(StatusTarefa status)
    {
        return status switch
        {
            StatusTarefa.EmAndamento => "Em Andamento",
            StatusTarefa.Pendente => "Pendente",
            StatusTarefa.Concluida => "Concluída",
            _ => status.ToString()
        };
    }
}
<h2>Detalhes da Tarefa</h2>
<div>
    <dl class="row">
        <dt class="col-sm-3">Titulo</dt>
        <dd class="col-sm-9">@Model.Titulo</dd>

        <dt class="col-sm-3">Descrição</dt>
        <dd class="col-sm-9">@Model.Descricao</dd>

        <dt class="col-sm-3">Status</dt>
        <dd class="col-sm-9">@FormatarStatus(Model.Status)</dd>

        <dt class="col-sm-3">Categoria</dt>
        <dd class="col-sm-9">@(Model.Categoria?.Nome ?? "Sem Categoria")</dd>

        <dt class="col-sm-3">Prioridade</dt>
        <dd class="col-sm-9">@Model.Prioridade</dd>

        <dt class="col-sm-3">Prazo</dt>
        <dd class="col-sm-9">@Model.Prazo.ToString("dd/MM/yyyy")</dd>

        <dt class="col-sm-3">Criado Em</dt>
        <dd class="col-sm-9">@Model.CriadaEm.ToString("dd/MM/yyyy  HH:mm")</dd>
    </dl>
</div>
<div class="mt-3">
    <a asp-action="Editar" asp-route-id="@Model.Id" class="btn btn-primary">Editar</a>
    <a asp-action="Index" class="btn btn-secondary">Voltar</a>
</div>